[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: tests requiring real external services (Ollama, HuggingFace inference, network).",
]
addopts = [
    "-ra",
    "--strict-markers",
]
